services:
  - type: web
    name: autopilot-api
    env: python
    region: oregon
    buildCommand: ""
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
    rootDir: backend
